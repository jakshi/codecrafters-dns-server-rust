# 2025-12-13

* Fix: the warnings
* Fix: the bug where branch without resolver would assign answers to new variable and immediately go out of scope - so answers were always empty for non-recursive queries
